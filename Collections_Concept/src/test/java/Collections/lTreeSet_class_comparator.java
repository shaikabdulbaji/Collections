package Collections;

import java.util.Comparator;

public class lTreeSet_class_comparator implements Comparator {		

	@Override
	public int compare(Object o1, Object o2) {
/*
		Integer ione = (Integer) o1;
		
		Integer itwo = (Integer) o2;
		
		return ione.compareTo(itwo); // Ascending Order
		return itwo.compareTo(ione); // Decending Order
		return +1; // This will print in Insertion Order
		return -1; // This will print in reverse Insertion Order

		
				String ione = (String)o1;
				
				String itwo = (String)o2;
				
				return ione.compareTo(itwo); // Ascending Order
				return itwo.compareTo(ione); // Decending Order
				return +1; // This will print in Insertion Order
				
	*/
				return -1; // This will print in reverse Insertion Order

	}

}
